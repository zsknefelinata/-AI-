# 前端设计方案 v1.0

> 项目：基于AI的路面病害智能检测与低碳养护系统
> 版本日期：2026-09-02
> 本文件只描述前端方案，不包含代码实现。

## 一、方案定位

产品形态不是展示型网站，而是面向县乡道路养护管理人员的 Web 业务系统。首页即工作台，用户打开系统后第一眼看到待办、任务状态和风险提示，随后沿“上传 → 识别 → 量化 → 决策 → 报告”的闭环完成巡检业务。

### 1.1 设计原则

- 工作优先：页面以数据密度和操作效率为主，不设置营销化大区。
- 闭环明确：每个页面只承担一个业务角色，导航顺序与业务闭环一致。
- 结果可复核：所有识别、量化、碳排结果都带状态、置信度或参数来源，并提供人工修正入口。
- 视觉中性：用橙、蓝、绿作为功能色，但以浅灰白中性色为主底，避免任何一个色系主导界面。
- 桌面优先：首期以 1280px 以上桌面浏览器为主，同时保证 768px 平板宽度可用。

## 二、信息架构与导航

### 2.1 页面路由

| 菜单 | 页面 | 路由 |
| --- | --- | --- |
| 工作台 | 首页总览 | `/` |
| 巡检业务 | 任务管理 | `/tasks` |
| 巡检业务 | 智能识别 | `/recognition` |
| 巡检业务 | 量化评估 | `/measure/:taskId` |
| 低碳养护 | 低碳决策 | `/decision/:taskId` |
| 数据分析 | 统计看板 | `/stats` |
| 报告中心 | 报告管理 | `/reports` |
| 系统设置 | 参数库与审计 | `/settings` |

### 2.2 整体布局

```
+-------------------------------------------------------------------------+
| 顶部栏 56px：产品名 | 全局搜索 | 通知 | 当前用户                          |
+---------+---------------------------------------------------------------+
| 侧边栏   | 内容区                                                         |
| 224px    | 面包屑 / 页面标题 / 页面级操作                                  |
|          |                                                               |
| 工作台   | 业务内容区（数据表格、图表、分栏工作区）                        |
| 任务管理 |                                                               |
| 智能识别 |                                                               |
| 量化评估 |                                                               |
| 低碳决策 |                                                               |
| 统计看板 |                                                               |
| 报告中心 |                                                               |
| 系统设置 |                                                               |
+---------+---------------------------------------------------------------+
```

- 侧边栏分组：工作台 / 巡检业务 / 低碳养护 / 数据分析与报告 / 系统。
- 顶部栏展示当前任务上下文，例如“任务 RD-20260902-003 / 路段 S301 K12+000”。
- 内容区最大宽度 1440px，页面上下间距 16px，不使用大页脚。

## 三、页面方案

### 3.1 首页工作台

首页要回答四个问题：今天有多少工作、哪些结果要复核、病害分布如何、低碳效益如何。

```
+-------------------------------------------------------------------------+
| 首页工作台                                                                |
| 4 个 KPI：今日任务 | 待复核 | 本月病害点数 | 累计预估减排量                |
+-------------------------------------------------------------------------+
| 快捷入口：新建任务 | 批量识别 | 生成报告 | 参数库                           |
+-------------------------------------------------------------------------+
| 病害类型分布图  |  路段分布图   |  病害月度趋势图                          |
| (环形图)        |  (条形图)     |  (折线图)                               |
+-------------------------------------------------------------------------+
| 最近任务表：任务号 | 路段 | 病害数 | 识别状态 | 复核状态 | 操作             |
+-------------------------------------------------------------------------+
| 风险提示条：待复核任务、无标定记录、异常任务                              |
+-------------------------------------------------------------------------+
```

内容要点：

- KPI 卡使用“数值 + 环比 + 跳转链接”，点击可进入对应列表。
- 快捷入口必须是带图标的主按钮，不写大段说明文字。
- 图表默认展示最近 30 天，支持时间筛选。
- 最近任务表默认显示 8 条，状态列有颜色标签，操作列固定为“打开”。

### 3.2 任务管理

任务管理是巡检业务入口，负责建任务、传图片、看进度、管理状态。

列表页结构：

```
+-------------------------------------------------------------------------+
| 工具栏：搜索任务号/路段 | 状态筛选 | 日期范围 | 新建任务                  |
+-------------------------------------------------------------------------+
| 批量上传区：拖拽或选择图片，支持文件夹压缩包                              |
+-------------------------------------------------------------------------+
| 表格：任务号 | 路段 | 图片数 | 病害数 | 识别状态 | 复核状态 | 更新时间 | 操作 |
| 状态：待识别 / 识别中 / 待复核 / 待决策 / 已完成 / 异常                   |
+-------------------------------------------------------------------------+
```

- 新建任务采用抽屉表单：任务名称、路段编号、采集日期、拍摄设备、备注。
- 批量上传后进入上传进度条，上传完成可一键“开始识别”。
- 表格支持多选，批量操作：开始识别、标记异常、归档。
- 点击任务行打开任务详情抽屉，详情页展示图片缩略图墙和状态流转记录。

### 3.3 智能识别

智能识别是核心工作页，采用“左侧图像工作区 + 右侧结果面板”的双栏布局。

```
+-------------------------------------------------------------------------+
| 任务选择：当前任务 | 图片序号 3/12 | 上一张 | 下一张                     |
+-------------------------------------------------------------------------+
| 左栏：原图 / 增强图 / 掩码叠加切换                                      |
|      质量检查 | 光照增强开关 | 缩放 | 拖拽                               |
+------------------------------------+------------------------------------+
| 右栏：检测结果                                                           |
|      病害列表：类别 | 置信度 | 像素面积 | 掩码状态                       |
|      图例：横向裂缝 / 纵向裂缝 / 龟裂 / 坑槽                             |
|      操作：进入量化 | 标记误检 | 重跑单图                                |
+-------------------------------------------------------------------------+
| 底部：识别队列进度条 | 完成 n/m | 失败原因                              |
+-------------------------------------------------------------------------+
```

内容要点：

- 图像切换以“原图 → 增强图 → 叠加图”三段式展示，方便答辩和人工比对。
- 每个病害结果都可单独标记“确认 / 误检 / 待复核”。
- 无病害图片显示“未检测到目标病害”，不能渲染空表格。
- 光照增强必须可关闭，方便验证增强前后效果。

### 3.4 量化评估

量化评估把掩码变成工程量，是“可复核”的关键页面。

```
+-------------------------------------------------------------------------+
| 状态条：标定模式 | 有效/降级输出 | 计量置信度                            |
+-------------------------------------------------------------------------+
| 左栏：图像 + 标尺/标定板叠加 + 透视网格                                  |
+------------------------------------+------------------------------------+
| 右栏：标定设置                                                           |
|      模式：已知相机参数 / 画面标尺 / 无标定                              |
|      标尺长度 | 相机高度 | 俯仰角                                        |
|-----------------------------------------------------------------------|
|      病害明细表：类型 | 长度/面积 | 宽度 | 密度 | 等级 | 可编辑           |
|      等级判定依据：裂缝宽度 / 面积占比 / 深度信息                         |
+-------------------------------------------------------------------------+
| 操作区：人工修正掩码 | 保存复核记录 | 生成低碳决策                        |
+-------------------------------------------------------------------------+
```

内容要点：

- 无标定时页首显示黄色警示条：“当前结果仅输出像素量与相对等级，不可用于实物量结算”。
- 病害明细表中长度、面积、等级字段均可编辑，编辑行为写入审计日志。
- 使用标尺模式时，允许用户拖拽标尺端点，实时重算换算比例。

### 3.5 低碳决策

低碳决策强调“方案比选可追溯”，页面采用参数区 + 对比表 + 图表三栏。

```
+-------------------------------------------------------------------------+
| 参数配置：病害规模 | 服务期 | 路线距离 | 材料/机械参数                   |
+-------------------------------------------------------------------------+
| 方案对比表：                                                            |
| 传统热补 / 预防性养护 / 自定义方案                                       |
| 成本 | 材料碳排 | 运输碳排 | 施工碳排 | 周期累计碳排 | 推荐             |
+-------------------------------------------------------------------------+
| 分项碳排堆叠柱状图 | 成本-碳排散点 | 推荐理由                           |
+-------------------------------------------------------------------------+
| 操作：保存方案 | 调整参数 | 生成报告                                      |
+-------------------------------------------------------------------------+
```

内容要点：

- 排放因子、运输距离、机械台班必须能人工修改，并在结果旁标注参数来源。
- 推荐理由区展示规则，例如“预防性养护周期累计碳排低于传统热补 34%”。
- 不允许写死减排比例，所有结论由参数重算，页面显示计算时间。

### 3.6 统计看板

统计看板用于日常巡检数据汇总，图表优先，明细表兜底。

```
+-------------------------------------------------------------------------+
| 筛选栏：时间范围 | 区县 | 道路等级 | 病害类型 | 刷新                    |
+-------------------------------------------------------------------------+
| 图表网格：                                                            |
| 病害类型占比(环形) | 路段分布(条形) | 月度趋势(折线)                   |
| 复核合格率(柱状)  | 工艺方案对比(堆叠) | 待处理事项(排行)              |
+-------------------------------------------------------------------------+
| 明细表：路段 | 病害数 | 平均等级 | 方案数 | 预估减排量 | 报告状态        |
+-------------------------------------------------------------------------+
```

- 图表全部使用 ECharts，颜色调色板统一。
- 点击图表柱子可下钻到对应任务列表。
- 明细表导出 CSV，报告导出走报告中心。

### 3.7 报告中心

报告中心负责模板选择、生成进度、下载历史。

```
+-------------------------------------------------------------------------+
| 模板区：巡检报告 | 低碳养护报告 | 项目总结报告                           |
| 每个模板显示：用途 | 输出格式 | 最近生成时间                            |
+-------------------------------------------------------------------------+
| 生成队列：任务号 | 模板 | 生成状态 | 开始时间 | 下载                     |
+-------------------------------------------------------------------------+
| 历史记录：报告名称 | 来源任务 | 参数版本 | 生成人 | 审计信息             |
+-------------------------------------------------------------------------+
```

- 生成前必须选择报告范围：单任务 / 多任务合并 / 全项目。
- 下载格式：PDF 和 Word，Word 必须保留参数明细与排放因子来源表。
- 报告详情页展示目录、图表、参数审计块，支持在线预览后下载。

### 3.8 系统设置（辅助页）

- 参数库：养护工艺库、排放因子库、模型版本管理。
- 工艺参数表：适用病害、适用等级、材料单耗、设备台班、费用模板。
- 排放因子表：因子名称、数值、单位、来源、年份、地区、修改记录。
- 审计日志：任务、识别、复核、参数修改、报告生成的操作流水。
- 系统设置只对管理员角色开放，界面采用表格为主，不做仪表盘化装饰。

## 四、通用交互与状态

| 状态 | 视觉 | 说明 |
| --- | --- | --- |
| 待识别 | 橙色标签 | 已上传但未进入识别 |
| 识别中 | 蓝色标签 + 进度条 | 显示当前图片序号 |
| 待复核 | 紫色标签 | 需要人工确认结果 |
| 待决策 | 青蓝色标签 | 量化完成，等待低碳决策 |
| 已完成 | 绿色标签 | 已生成报告或归档 |
| 异常 | 红色标签 | 文件损坏、格式错误、接口失败 |

- 所有加载状态使用骨架屏，不使用无意义转圈动画。
- 空数据状态显示简短文案和推荐操作，例如“暂无病害，返回上一张”。
- 上传失败必须给出具体原因，不允许只显示“失败”。
- 重要操作（删除、覆盖参数、导出含个人信息的报告）需二次确认。
- 页面内所有图标按钮必须有 tooltip；有图标的按钮优先图标，不加冗余文字。

## 五、视觉规范

### 5.1 颜色

| 用途 | 色值 |
| --- | --- |
| 页面背景 | `#F5F6F4` |
| 卡片/面板背景 | `#FFFFFF` |
| 主文本 | `#1F2937` |
| 次级文本 | `#6B7280` |
| 边框/分割线 | `#E5E7EB` |
| 道路巡检主色（橙） | `#D97706` |
| AI识别主色（蓝） | `#2563EB` |
| 低碳主色（绿） | `#16A34A` |
| 风险/异常（红） | `#DC2626` |
| 页面标题 | `#111827`，20px/24px |

### 5.2 字体与间距

- 字体栈：`PingFang SC, Microsoft YaHei, HarmonyOS Sans SC, sans-serif`。
- 正文 14px，表格 13px，KPI 数值 24px，页面标题 20px，卡片标题 16px。
- 字距保持 0，标题不用负字距。
- 卡片圆角统一 8px，按钮圆角 6px，全站不超过 8px。
- 内容区左右间距 16px，卡片间距 12px，表格行高 40px。

### 5.3 组件规范

- 按钮：主按钮（橙）、次按钮（白底描边）、危险按钮（红底）、图标按钮。
- 表格：固定第一列操作入口，宽表格横向滚动，保留列宽稳定。
- 图表：ECharts 5，统一 `#D97706 #2563EB #16A34A #DC2626 #7C3AED` 调色板。
- 图标：lucide-vue-next，避免手绘 SVG。
- 面板：只对真正的独立工具/弹窗使用卡片；页面分区用全宽带和分隔线，禁止卡片套卡片。

## 六、技术选型与项目结构（实现阶段）

建议技术栈：

- Vue 3 + Vite + TypeScript
- Vue Router 4 + Pinia 2
- Element Plus 组件库
- ECharts 5（统计看板与决策图表）
- lucide-vue-next 图标
- axios + 本地 Mock（首期可用 `vite-plugin-mock` 提供演示数据）

建议目录：

```
src/
  api/
  assets/
  components/
    common/
    charts/
    drawing/
  layouts/
    MainLayout.vue
  stores/
  types/
  views/
    dashboard/
    tasks/
    recognition/
    measure/
    decision/
    stats/
    reports/
    settings/
```

## 七、实施顺序

| 阶段 | 页面 | 完成标准 |
| --- | --- | --- |
| P0 | 工作台、任务管理、智能识别 | 能建任务、传图、跑通识别结果展示 |
| P1 | 量化评估、低碳决策 | 有标定/无标定流程、方案对比可改参数 |
| P2 | 统计看板、报告中心、系统设置 | 全页面可演示，导出报告、审计日志可用 |

## 八、验收要点

- 1280px 桌面与 768px 平板下无文字溢出、无控件重叠。
- 首页到报告中心的主流程可在 5 步内完成，并可回退复核。
- 所有状态均有明确视觉反馈；异常输入有具体错误信息。
- 图表可交互、可筛选，数据口径与后端/手算一致。
- 前端结构按上面目录实现，接口数据格式与后端约定后补充 API 文档。
